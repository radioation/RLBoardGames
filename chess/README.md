# Readme
This is not ready yet. Currently
1. Reworking for next version of retro.link (emulated for now).
  
2. Needs UI for pawn promotion (no keyboard, so probably check 
  pawns reacing last row))

